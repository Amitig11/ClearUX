# Demo pages

The web app renders the three demo pages inside the interface. This folder is included so the repository structure is clear if the team later wants to add separate HTML pages for more services.
